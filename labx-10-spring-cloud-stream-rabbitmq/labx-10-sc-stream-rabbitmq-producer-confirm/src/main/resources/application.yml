spring:
  application:
    name: demo-producer-application # 应用名称
  cloud:
    # Spring Cloud Stream 配置项，对应 BindingServiceProperties 类
    stream:
      # Binder 配置项，对应 BinderProperties Map
      binders:
        rabbit001:
          type: rabbit # 设置 Binder 的类型
          environment: # 设置 Binder 的环境配置
            # 如果是 RabbitMQ 类型的时候，则对应的是 RabbitProperties 类
            spring:
              rabbitmq:
                host: 192.168.177.4 # RabbitMQ 服务的地址
                port: 5672 # RabbitMQ 服务的端口
                username: mqadmin # RabbitMQ 服务的账号
                password: mqadmin # RabbitMQ 服务的密码
                publisher-returns: true # 设置消息是否回退，默认为 false
                publisher-confirm-type: simple # 设置开启消息确认模型，即，使用同步的 Confirm 模式。默认为 null 不进行确认
      # Binding 配置项，对应 BindingProperties Map
      bindings:
        demo01-output:
          destination: DEMO-TOPIC-01 # 目的地。这里使用 RabbitMQ Exchange
          content-type: application/json # 内容格式。这里使用 JSON
          binder: rabbit001 # 设置使用的 Binder 名字
          producer:
            error-channel-enabled: true # 是否开启异常 Channel，默认为 false 关闭，因为当消息发送被退回时，Spring Cloud Stream RabbitMQ 是发送一个本地异常消息到相应的异常 Channel。
      # RabbitMQ 自定义 Binding 配置项，对应 RabbitBindingProperties Map
      rabbit:
        bindings:
          demo01-output:
            # RabbitMQ Producer 配置项，对应 RabbitProducerProperties 类
            producer:
              confirm-ack-channel: demo01-producer-confirm # 设置发送确认的 Channel，默认为 null

server:
  port: 18080 # 端口
