<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 环境配置，可以配置多个环境 -->
    <environments default="chat02">
        <!--
            environment用来对某个环境进行配置
            id：环境标识，唯一
         -->
        <environment id="chat02">
            <!-- 事务管理器工厂配置org.apache.ibatis.transaction.TransactionFactory接口，实现：
                org.apache.ibatis.transaction.managed.ManagedTransactionFactory,
                org.apache.ibatis.transaction.jdbc.JdbcTransactionFactory
             -->
            <transactionManager type="org.apache.ibatis.transaction.jdbc.JdbcTransactionFactory"/>
            <!-- 数据源工厂配置，使用工厂来创建数据源org.apache.ibatis.datasource.DataSourceFactory接口，实现：
             org.apache.ibatis.datasource.jndi.JndiDataSourceFactory,
             org.apache.ibatis.datasource.pooled.PooledDataSourceFactory,
             org.apache.ibatis.datasource.unpooled.UnpooledDataSourceFactory
             -->
            <dataSource type="org.apache.ibatis.datasource.pooled.PooledDataSourceFactory">
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://192.168.177.1:3306/lab-12-spring-mybatis?characterEncoding=UTF-8"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="mapper/UserMapper.xml" />
    </mappers>
</configuration>